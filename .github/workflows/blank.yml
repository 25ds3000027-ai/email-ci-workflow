name: Email CI Workflow

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 25ds3000027@ds.study.iitm.ac.in
        run: echo "Hello from GitHub Actions!"
